import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'manuel.salazar.pheva1',
  appName: 'manuel-salazar-pheva1',
  webDir: 'www'
};

export default config;
